{
  "name": "autocomplete-en-cn",
  "main": "./lib/autocomplete-en-cn",
  "version": "0.2.0",
  "description": "English word autocompetion with Chinese explanation",
  "repository": "https://github.com/wushuaibuaa/autocomplete-en-cn.git",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "configSchema": {
    "FileType": {
      "type": "string",
      "default": ".text.plain, .text.plain.null-grammar, .text.tex.latex, .source.gfm",
      "description": "List of scopes for typing which will be given the word completion. See [the README](https://github.com/atom/) for more information on finding the correct scope for a specific language."
    }
  },
  "dependencies": {
    "fuzzaldrin": "^2.1.0"
  },
  "providedServices": {
    "autocomplete.provider": {
      "versions": {
        "2.0.0": "getProvider"
      }
    }
  }
}
